<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zap.marvygroup">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MarvyGroupApplication"
        android:allowBackup="true"
        android:requestLegacyExternalStorage="true"
        android:icon="@drawable/marvy_app_sticker"
        android:label="@string/app_name"
        android:roundIcon="@drawable/marvy_app_sticker"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Sunflower"
        android:usesCleartextTraffic="true">

        <activity android:name=".ui.notification.NotificationActivity"
                  android:parentActivityName=".HomeActivity"
            >

        </activity>
        <activity
            android:name=".ui.documents_type.Documents"
            android:parentActivityName=".HomeActivity" />
        <activity android:name=".ui.SalaryActivity" />
        <activity
            android:name=".ui.splash.MarvySplash"
            android:theme="@style/Base.Theme.Sunflower">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.auth.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/Base.Theme.Sunflower" />
        <activity
            android:name=".ui.apply.ApplyForm"
            android:label="@string/title_activity_apply_form"
            android:parentActivityName=".ui.auth.LoginActivity"
            android:theme="@style/Base.Theme.Sunflower" />
        <activity
            android:name=".ui.reset.ForgotPassword"
            android:label="@string/app_name"
            android:parentActivityName=".ui.auth.LoginActivity"
            android:theme="@style/Base.Theme.Sunflower" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/Base.Theme.Sunflower" />
        <activity android:name=".ui.ShowSalaryActivity" />

        <meta-data
            android:name="android.webkit.WebView.EnableSafeBrowsing"
            android:value="false" />

        <provider
            android:name=".MyFileprovider"
            android:authorities="com.freshdesk.helpdesk.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_path" />
        </provider>

        <!-- The below code is for android OS version below N -->
        <receiver
            android:name=".util.NetCheckerReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>